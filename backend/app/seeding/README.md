# Compatibility seed package

Implementation and current commands live in [backend/seeding](../../seeding/README.md).

This package contains one-way compatibility imports only. New generator, planner,
runner, reference and scenario work belongs in the sibling `seeding` package.
Reporting transformations belong in the sibling `reporting` package.
